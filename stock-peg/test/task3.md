再次检查server网络获得所有股票数据和财务数据（包括最新年报季报数据， 过去三年季报数据）、上证、深证，美国市场指数，server启动后从网络获得或更新，所有数据获得后均存储在server；，client启动后优先采用server上提前获得数据（但时，会请server异步检查更新，如果必要更新就更新）。另外，client不直接从外部网络获取数据，所有数据都从server获取，server有单独的接口！检查上述要求的完成度！


财务数据仍然看不到，是否时server上没有，如何没有serer应自动收集并推送给client!!!PMR系列图没有显示，另外参照MA20,EMA20,增加EMA60,MA120, EMA120,在K形图缩小放大时，三张图应该同步，当前不同步!!!

在K形图缩小放大时，三张图应该同步，并且以最右侧为中心缩放，因最右侧时间是当前时间另外；采用”趋势模式“时，应将MA5,MA10,MA30关闭；ui界面上，rightPanel当分割线不移动时，RightPanel内容仍然随着鼠标移动！rightPanel内部只跟随分割线移动！确保headbar指数数据缺失时，server应自动更新并推送相关数据！

当前，server更新策略：首先需要检查是否存在当前本地没有的股票，如果存在优先检查和更新，其他股票则按时间顺序更新。
EMA曲线颜色同MA曲线颜色，但是EMA采用DOTPLINE线型。

市场情绪（放大版）中成交额没有图，没有相关数据，分析原因？

02:18
weiliang cao

K形图的主图右侧不应留空间,最新时间应靠右侧，最左侧的时间应和副图最左侧时间是相同的！

K形图的主图为什么没有靠右侧，水平轴最新时间为什么靠不了右侧？K形图的主图的时间起始点为什么和副图不一致？
