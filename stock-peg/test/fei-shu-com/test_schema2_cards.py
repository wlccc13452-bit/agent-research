#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
测试 Schema 2.0 新设计卡片
"""

import sys
import os
import json
from pathlib import Path
from datetime import datetime
from typing import Dict, Any, List

def test_schema2_cards():
    """测试 Schema 2.0 新设计卡片"""
    
    使用方法:
    python test_schema2_cards.py
    """
    测试步骤：
    1. 运行测试脚本
            python test_schema2_cards.py [chat_id]
            
            使用方法：
            python test_schema2_cards.py ou_xxxxxxxxxxxx}
            python test_schema2_cards.py oc_xxxxxxxxxxxx
            print(f"\n✅ 持仓卡片已发送到飞书")
            print(f"✅ 关注列表卡片已发送到飞书")
            print(f"\n📱 请在手机飞书中测试:")
            print("  1. 卡片是否正常显示")
            print("  2. 点击股票查看详情")
            print("  3. 测试底部操作按钮（返回主菜单、刷新等）")
            print("\n💡 测试完成，        }    ]
    }
}

